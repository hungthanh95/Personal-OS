name: personal_os
description: A local-first mission control for meaningful work.
publish_to: none
version: 0.4.0+4
environment:
  sdk: ^3.12.2
dependencies:
  flutter:
    sdk: flutter
  sqflite_common_ffi: ^2.4.0
  path_provider: ^2.1.5
  file_selector: ^1.1.0
  archive: ^3.6.1
  xml: ^6.6.1
  path: ^1.9.1
  flutter_markdown: ^0.7.7+1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/strategy_starter.json
